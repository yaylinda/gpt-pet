import {TabbedScreenWrapper} from '@common/ScreenWrapper';

const TodayScreen = () => {
    return (
        <TabbedScreenWrapper>

        </TabbedScreenWrapper>
    );
};

export default TodayScreen;
