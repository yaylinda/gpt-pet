import {TabbedScreenWrapper} from '@common/ScreenWrapper';

const ProfileScreen = () => {
    return (
        <TabbedScreenWrapper>

        </TabbedScreenWrapper>
    );
};

export default ProfileScreen;
