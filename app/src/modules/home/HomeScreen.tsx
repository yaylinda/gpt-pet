import {FullHeightScreenWrapper, TabbedScreenWrapper} from '@common/ScreenWrapper';

const HomeScreen = () => {
    return (
        <FullHeightScreenWrapper>

        </FullHeightScreenWrapper>
    );
};

export default HomeScreen;
